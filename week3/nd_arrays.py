#24331a05d8
#cREATING nUMpY aRRAYS
import numpy as nd
n=nd.array([1,2,3,4])
m=nd.array([[5,6],[7,8]])
x=nd.array({1,2,3,4})           #Using set datatype in NumPy array
print(n)
print(m)
print(x)

#nd is the shortcut to use numpy keyword
#To create a Multi-Dimensional array, we need to use nested datatype,like nested tuple,nested list etc..
#We can't create two seperate arrays with a single call, multiple initialisations are needed to call multiple arrays.